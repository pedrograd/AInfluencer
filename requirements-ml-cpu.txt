# PHASE 3: ML/CPU requirements - optional but recommended
# These may fail on Python 3.13+ or without proper build tools
# Install only if Python version is compatible (3.11 or 3.12 recommended)

# CPU runtime (more compatible than GPU version)
onnxruntime>=1.16.0

# Face analysis (may require onnxruntime)
# insightface>=0.7.3  # Commented - install manually if needed

# Image upscaling (optional, may fail to build)
# realesrgan>=0.3.0  # Commented - requires basicsr which may fail
# gfpgan>=1.3.8      # Commented - requires basicsr which may fail

# Metadata handling
piexif>=1.1.3

# Note: If you need GPU acceleration:
# - Install onnxruntime-gpu instead of onnxruntime
# - Ensure CUDA/cuDNN is properly installed
# - Python 3.11 or 3.12 recommended for best compatibility
